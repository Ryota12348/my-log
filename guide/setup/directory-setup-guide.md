# directory_setup.ps1 使い方ガイド

このファイルは、 ディレクトリ構造を一括で作成する PowerShell スクリプトです。
フォルダと空の `.md` ファイルを自動生成します。

---

## 📁 事前準備

1. `directory_setup.ps1` を作成する
2. デスクトップなど任意の場所に保存する
3. 文字コードは **UTF-8（BOM付き推奨）** で保存する

---

## ▶ 実行手順

### ① PowerShell を開く

* Windowsキー → 「PowerShell」と検索 → 起動

---

### ② スクリプト保存場所へ移動

例（デスクトップにある場合）：

```powershell
cd $HOME\Desktop\my-log
```

※ `my-log` は保存したフォルダ名に合わせて変更

---

### ③ 実行ポリシーの設定（初回のみ）

スクリプトが実行できない場合：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

確認が出たら `Y` を入力

---

### ④ スクリプト実行

```powershell
.\directory_setup.ps1
```

成功すると：

```
ディレクトリ構造が作成されました！
```

と表示されます。

---

## 📂 作成される構造

```
study/
 ├─ math/
 ├─ japanese/
 ├─ english/
 ├─ science/
 ├─ social/
 └─ info/
```
> [!NOTE]
>ファイル構造の図を作るには  
>[https://dir-maker.app/](https://dir-maker.app/)  
>で。


* notebook フォルダ
* textbook-data.md
* まとめ系フォルダ
* 日付ノート（例：2026-02-16.md）

が自動生成されます。

---

## 🛠 トラブル対処

### ❌ 文字化けエラーが出る場合

* スクリプトを **UTF-8で保存し直す**
* 文字列を `'シングルクォート'` にする

### ❌ 実行できない場合

* 実行ポリシーを確認
* PowerShell を再起動

---


これで `setup_study.ps1` の基本操作はOKです。

### コード例
<details>
<summary>クリックで開く</summary>

```powershell
# フォルダ作成
$dirs = @('study\math\notebook',
    'study\japanese\重要表現まとめ',
    'study\japanese\notebook',
)

foreach ($d in $dirs) {
    New-Item -ItemType Directory -Path $d -Force | Out-Null
}

# 空ファイル作成
$files = @(
    'study\math\公式まとめ.md',
    'study\math\textbook-data.md',
    'study\math\notebook\2026-02-16.md',
)
foreach ($f in $files) {
    New-Item -ItemType File -Path $f -Force | Out-Null
}

Write-Host "Studyディレクトリ構造が作成されました！"

```
</details>